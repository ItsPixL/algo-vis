// src/App.tsx

// Imports
import { motion } from "motion/react";

// App
export const App = () => {
  return (
    <motion.div className="bg-slate-950 w-screen h-screen">
      <motion.div>Hello</motion.div>
    </motion.div>
  );
};
